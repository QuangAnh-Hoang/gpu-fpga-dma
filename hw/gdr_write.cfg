[connectivity]
# Wire the kernel's AXI master to HOST[0] = the XDMA slave bridge (HMSS).
# Writes through this port become PCIe TLPs whose destination is set by the
# HMSS page table (which our driver points at GPU BAR bus addresses).
sp=gdr_write_1.m_axi_gmem:HOST[0]
