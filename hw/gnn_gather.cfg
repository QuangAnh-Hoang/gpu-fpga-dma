[connectivity]
# sq (node-id reads) + out (ring/CQ writes) target the 16MB GPU-VRAM window
# behind the HMSS slave bridge; feat is the static HBM feature table.
sp=gnn_gather_1.sq:HOST[0]
sp=gnn_gather_1.feat:HBM[4:31]
sp=gnn_gather_1.out:HOST[0]
